---
title: M7.1 OTLP HTTP + collector CI — buildable spec rev1
task_id: 260624_m7.1-otlp-http
depends_on_epic: 2627
---

# M7.1 OTLP HTTP + collector CI — buildable spec (rev1)

> **CI promotion (shipped):** M7.1 delivered `otlp-collector-advisory`. Job is now **blocking**
> as `otlp-collector` (M7.2, `1ef43af`). See
> [CI promotion status](260623_ci-promotion-status.md).

**Goal:** HTTP/protobuf OTLP egress + advisory otel-collector CI smoke; gRPC remains default.

## Delivered

| ID | Deliverable |
|----|-------------|
| M7.1a | `CISTERNA_OTLP_PROTOCOL` (`grpc` default, `http`) |
| M7.1b | `opentelemetry-exporter-otlp-proto-http` in `[otlp]` extra |
| M7.1c | `tests/test_otlp_http.py` unit + integration (skipped without collector) |
| M7.1d | `export-dogfood.yml` `otlp-collector-advisory` job (`continue-on-error: true`) — **promoted to blocking `otlp-collector` in M7.2** |

## Env contract

| Variable | Default | Values |
|----------|---------|--------|
| `CISTERNA_OTLP_ENDPOINT` | unset | OTLP endpoint URL |
| `CISTERNA_OTLP_PROTOCOL` | `grpc` | `grpc`, `http`, `http/protobuf` |

HTTP convention: port **4318** (gRPC **4317**).

## AC matrix

| AC | Then |
|----|------|
| AC-M7.1-0 | Default suite green without collector (integration skipped) |
| AC-M7.1-1a | `resolve_otlp_protocol()` maps http variants → `http` |
| AC-M7.1-1b | `CISTERNA_OTLP_PROTOCOL=http` attaches `OtlpExporter` |
| AC-M7.1-1c | `create_otlp_span_exporter(..., protocol=http)` uses HTTP exporter class |
| AC-M7.1-2 | Advisory CI runs integration tests against docker collector |
