---
workspace: cisternal
session_id: 9c577a50-0c24-4b76-90d6-9756e177dec3
status: complete
immediately_relevant:
- file: src/cisternal/export/cursor.py
  lines:
  - 1
  - 20
  reason: CursorEmitter now double-emits skills at both skills/<name>/SKILL.md (IDE) and .cursor/skills/<name>/SKILL.md (CLI) -- module docstring explains why with the live verification evidence
- file: src/cisternal/export/marketplace.py
  lines:
  - 1
  - 30
  reason: content_version()/merge_marketplace_entry()/plugin_output_dir() backing `cisternal assets publish-shared` -- the shared-marketplace publish path, distinct from `assets install`'s standalone self-marketplace path
- file: src/cisternal/cli.py
  lines:
  - 370
  - 400
  reason: assets install docstring now cross-references publish-shared; read both docstrings together to understand the split before adding a third marketplace-related command
- file: src/cisternal/assets/manifest.py
  lines:
  - 82
  - 145
  reason: _load_skill_resources -- walks references/scripts/assets/ sibling dirs next to a skill's SKILL.md; this is what makes bundled skill resources actually reach an exported plugin
- file: .praxia/skills/customizing-tools/SKILL.md
  lines:
  - 1
  - 30
  reason: the dogfood skill this session authored and shipped -- references/decision-matrix.md and the 5 Rust-migration reference docs are the real deliverable content
active_dispatches: []
task_id: 260813_cursor-skills-marketplace-publish
phase: shipped
goal_statement: Make cisternal's plugin-export pipeline actually deliver a bundled plugin's skills to the cursor-agent CLI, then give cisternal itself a real, working install/publish path into a local Claude Code marketplace, dogfooded with a genuinely new skill.
context_refs: []
failed_attempts:
- attempt_id: a1
  description: scripts/record_decision.py's first version called init_pipeline() + pipeline.emit() then let the script exit normally, with no explicit shutdown_pipeline() call
  outcome: The telemetry event was silently never written to ~/.cisternal/logs/ -- confirmed by grepping the JSONL file after running the script and finding zero matching lines
  reason_failed: EventPipeline's consumer thread is a daemon thread with a non-blocking queue.put_nowait(); a short-lived script process exits and kills the daemon thread before it ever dequeues and writes the event. shutdown_pipeline() drains the queue and joins the listener (bounded by its own timeout) before returning.
  do_not_retry: true
  evidence_ref: fixed in the same session; see _emit_one_shot()'s docstring in .praxia/skills/customizing-tools/scripts/record_decision.py
open_questions: []
deferred:
- description: Consolidate myxcel's scripts/marketplace/sync_marketplace.py (a hand-copied duplicate) to call the new shared cisternal.export.marketplace module instead
  rationale: Cross-repo change, out of scope for a cisternal-only session; flagged to the user as an option, not requested
  recommended_phase: null
- description: Investigate whether praxia's Rust bundle_cursor.rs has the same .cursor/skills/ discovery gap cisternal's Python CursorEmitter had
  rationale: Would require touching praxia's repo and would break the frozen rust-parity conformance pin unless done deliberately in coordination with that pin
  recommended_phase: null
lessons_refs:
- .praxia/lessons.jsonl
next_steps:
- Consider whether praxia's own bundle_cursor.rs (the Rust source cisternal's rust-parity emitters mirror) has the identical .cursor/skills/ discovery gap this session fixed in cisternal's Python ClaudeEmitter -- out of scope this session since changing it would break the frozen conformance pin against a specific praxia commit.
- Consider porting myxcel's hand-rolled scripts/marketplace/sync_marketplace.py to call cisternal.export.marketplace / `cisternal assets publish-shared` directly instead of vendoring a duplicate copy of the same merge algorithm.
- 'If cisternal ever wants CLI-agnostic skill delivery, note the underlying finding: cursor-agent''s --plugin-dir and even a real local install under ~/.cursor/plugins/local/ do NOT surface plugin-bundled skills at all -- only .cursor/skills/<name>/SKILL.md (project) or ~/.cursor/skills/<name>/SKILL.md (user) are auto-discovered by the CLI. This is a Cursor-CLI-side limitation, not something cisternal''s emitter can further work around beyond what it now does.'
---
