[sprint]
sprint_id = "260623_m32-entry-points"
sprint_num = 5
name = "Cisterna M3.2 — entry_point emitter registry"
description = "Four waves: registry + entry-points → validate refactor → CLI refactor → golden regression (#2563)."
context = '''
Spec: .praxia/docs/specs/260623_m32-buildable-spec-rev1.md
Baseline: 275 pytest green. Zero golden drift required.
Stack: uv run pytest; uv run ruff check .
'''
max_fix_retries = 1

[[tracks]]
id = "w1"
is_research = false

[[tracks.items]]
backlog_id = "2563"
phase_name = "M3.2-W1 — registry + entry-points"
fixer_prompt = '''
task_id: 260623_m32-entry-points

Implement export/registry.py per rev1 spec L29-L35:
- list_emitter_surfaces(), get_emitter(surface, emit_command_bodies=False)
- Factory callables for claude/cursor/copilot/antigravity
- pyproject.toml [project.entry-points."cisterna.emitters"]
- tests/test_export_registry.py AC-M32-1..5

Commit: feat(export): emitter registry via entry_points (#2563 W1)
'''
reviewer_prompt = '''
VERIFY: entry_points load 4 surfaces; get_emitter claude matches old default; unknown returns None.
pytest green; ruff clean. PASS if satisfied.
'''

[[tracks]]
id = "w2"
is_research = false

[[tracks.items]]
backlog_id = "2563"
phase_name = "M3.2-W2 — validate_golden refactor"
fixer_prompt = '''
task_id: 260623_m32-entry-points

Refactor validate_golden.py: remove _EMITTERS dict; use get_emitter.
AC-M32-6 validate tests still pass.

Commit: refactor(export): validate golden uses emitter registry (#2563 W2)
'''
reviewer_prompt = '''
VERIFY: all validate golden tests pass unchanged.
PASS if satisfied.
'''

[[tracks]]
id = "w3"
is_research = false

[[tracks.items]]
backlog_id = "2563"
phase_name = "M3.2-W3 — CLI dispatch refactor"
fixer_prompt = '''
task_id: 260623_m32-entry-points

cli.py export + validate: use list_emitter_surfaces/get_emitter;
exit 2 on None. export __init__ exports registry helpers.

Commit: refactor(cli): export/validate use emitter registry (#2563 W3)
'''
reviewer_prompt = '''
VERIFY: export --surface for all 4 surfaces works; unknown surface exit 2.
PASS if satisfied.
'''

[[tracks]]
id = "w4"
is_research = false

[[tracks.items]]
backlog_id = "2563"
phase_name = "M3.2-W4 — golden regression"
fixer_prompt = '''
task_id: 260623_m32-entry-points

tests/test_export_regression_m32.py — all 5 golden digests unchanged (AC-M32-7).
Full suite green.

Commit: test(export): M3.2 golden regression (#2563 W4)
'''
reviewer_prompt = '''
VERIFY: AC-M32-7; 275+ tests; ruff clean.
PASS if satisfied.
'''

[orchestration]
sequential = ["w1", "w2", "w3", "w4"]
concurrent = []
